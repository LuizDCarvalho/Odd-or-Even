# Even or Odd Checker

Simple JavaScript function that checks if a number is even or odd.

## How it works
- If number % 2 === 0 → Even
- Else → Odd

## Example
console.log(checkNum(2)) // Even
console.log(checkNum(1)) // Odd
